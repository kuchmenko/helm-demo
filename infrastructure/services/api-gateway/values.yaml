replicaCount: 2 # default replicas (dev)

image:
  tag: "{{ .Values.global.gitSha }}"
service:
  port: 8080

resources:
  limits: { cpu: 800m, memory: 512Mi }
  requests: { cpu: 300m, memory: 256Mi }

terminationGrace: 30

config:
  API_GW_VALUE: "60000"
